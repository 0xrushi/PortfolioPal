export function OnboardingNote() {
  return (
    <div className="flex flex-col space-y-4 bg-green-700 p-2 rounded text-stone-200 text-sm">
      <span>
        Only 10 total attempts are available per day globally (shared by
        everyone).
      </span>
    </div>
  );
}
