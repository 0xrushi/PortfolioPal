# Copy this file to .env and fill in your values.
# docker compose up will use this automatically.

# ─── Host & Ports ────────────────────────────────────────────────────────────
# Set HOST_IP to the IP/hostname where the backend is reachable from browsers.
# Use your LAN IP (e.g. 192.168.1.153), a domain name, or 127.0.0.1 for local-only.
HOST_IP=127.0.0.1
BACKEND_PORT=7001
FRONTEND_PORT=5173

# ─── Admin credentials (for theme/portfolio save) ─────────────────────────────
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# ─── LLM API Keys ─────────────────────────────────────────────────────────────
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
